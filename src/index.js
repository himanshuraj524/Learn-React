import ReactDOM from 'react-dom';
// This is the main File of the react project which is basically act like the root of the project where the App component will get add.
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
